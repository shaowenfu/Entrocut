# Core Shell

`core` 当前是最小 `Local Service Shell（本地服务壳层）`。

## 当前能力

1. `GET /health` 健康检查。
2. `POST /api/v1/ingest` 占位。
3. `POST /api/v1/search` 占位。
4. `POST /api/v1/render` 占位。

## 说明

旧 `Pipeline（管线）` 代码（切分、抽帧、渲染、Mock 客户端）已全部清理。
