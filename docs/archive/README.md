# Docs 归档说明

这个目录用于存放项目推进过程中的阶段性中间文档。

归档原则：

1. `docs/` 根目录只保留当前仍直接服务开发的主文档
2. 某一阶段用于收敛状态、讨论方案、联调跟进的中间文档，在阶段结束后移入这里
3. 归档文档不删除，保留决策过程和历史上下文

当前归档批次：

1. `2026-03-rebuild-phase`
   - 对应“清场式重构后，重建前端状态模型并打通最小闭环”的阶段
