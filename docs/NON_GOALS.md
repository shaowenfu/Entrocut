# Non-goals

当前基线阶段明确不做以下事项：

1. 不实现 `AI Tool（智能体工具）` 与 `Skill（技能）` 执行链路。
2. 不实现 `Vector Retrieval（向量检索）` 真实算法。
3. 不实现 `Timeline Editing（时间线编辑）` 行为。
4. 不实现 `Export Pipeline（导出管线）`。
5. 不保留任何历史 `Mock` 与 `Round` 代码路径。
